#include "application/Session.hpp"

namespace Application {

// Empty for now as logic is in header/unique_ptr default ctor
// But required for the library build target if it's not interface
void Session_KeepAlive() {}

} // namespace Application
