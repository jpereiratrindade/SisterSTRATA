# Territorial Hypothesis Schema Definition (v1.1)
# Format: JSON (Proposed)

# Context:
# A "Territorial Hypothesis" defines rules and constraints under which land-use potentials 
# are evaluated and spatially allocated. It does NOT prescribe a final state, but proposes 
# a candidate configuration for coherence testing.

{
  "hypothesis_id": "string (UUID or Slug)",
  "name": "string (Display Name, e.g., ' Agroecological Transition 2030')",
  "description": "string (Optional context)",
  
  # Protection: Explicit intent of the hypothesis.
  "hypothesis_type": "string (enum: 'Exploratory', 'Scenario', 'ConstraintTest')",

  "land_use_types": [
    {
      "id": "campestre",
      "name": "Campestre",
      "color_hex": "#E8D865", // Pale yellow/green
      "constraints": {} // Open field constraints
    },
    {
      "id": "florestal_natural",
      "name": "Florestal Natural",
      "color_hex": "#228B22", // Forest Green
      "constraints": {
        "max_slope_degrees": 45.0
      }
    },
    {
      "id": "florestal_cultivado",
      "name": "Florestal Cultivado",
      "color_hex": "#8FBC8F", // Dark Sea Green
      "constraints": {
        // Example: Planted forests might have specific slope limits for machinery
        "max_slope_degrees": 25.0 
      }
    },
    {
      "id": "antropico_urbano",
      "name": "Antrópico + Urbano",
      "color_hex": "#808080", // Grey
      "constraints": {
        "prohibited_soil_orders": ["Gleissolo"] // Avoid wetlands
      }
    },
    {
      "id": "agua",
      "name": "Água",
      "color_hex": "#4682B4", // Steel Blue
      "constraints": {}
    }
  ],

  "allocation_rules": [
    {
      "priority": 1,
      "land_use_id": "florestal_natural", // Updated reference
      "method": "GlobalConstraint",
      "parameters": {
        "slope_min": 20
      }
    }
  ]
}

# Implementation Notes:
# 1. This file will be parsed by a future `HypothesisLoader` (Infrastructure Layer).
# 2. The `LandUseCompatibilityService` will use the `constraints` block to evaluate individual pixels.
# 3. The `allocation_rules` will be used by the `Territory` to generate a candidate spatial configuration.
# 4. Input: Hypothesis -> Output: Coherence Evaluation. The system never "decides" on behalf of the user.
